import { SectionProp } from "../../types/type";

export default function MainScreen({ width }: SectionProp) {
  return (
    <section className="h-full bg-blue-100" style={{ width: `${width}%` }}>
      asdasd
    </section>
  );
}
